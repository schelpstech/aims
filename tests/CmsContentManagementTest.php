<?php

declare(strict_types=1);

use App\Authorization\PermissionCheckerInterface;use App\Security\SafeHtml;use App\Services\Cms\CmsMediaStorage;use App\Services\Cms\CmsRepositoryInterface;use App\Services\Cms\CmsService;
require dirname(__DIR__).'/vendor/autoload.php';$assert=static function(bool$c,string$m):void{if(!$c)throw new RuntimeException($m);};
try{$id='10000000-0000-4000-8000-000000000001';$repo=new class implements CmsRepositoryInterface{public int$writes=0;public array$last=[];public function publicPosts(string$t):array{return[['title'=>'Published','slug'=>'published','status'=>'published']];}public function publicPost(string$t,string$s):?array{return null;}public function publicPage(string$s):?array{return null;}public function publicLibrary():array{return['resources'=>[],'downloads'=>[],'faqs'=>[]];}public function publicMedia(string$i):?array{return null;}public function publicDownload(string$s,DateTimeImmutable$n):?array{return null;}public function adminDashboard():array{return[];}public function savePage(int$a,?string$i,array$d,DateTimeImmutable$n):string{$this->writes++;$this->last=$d;return'10000000-0000-4000-8000-000000000001';}public function savePost(int$a,?string$i,array$d,DateTimeImmutable$n):string{$this->writes++;$this->last=$d;return'10000000-0000-4000-8000-000000000001';}public function saveFaq(int$a,?string$i,array$d,DateTimeImmutable$n):string{$this->writes++;return'10000000-0000-4000-8000-000000000001';}public function createMedia(int$a,array$d,DateTimeImmutable$n):string{$this->writes++;return'10000000-0000-4000-8000-000000000001';}public function updateMediaStatus(int$a,string$i,string$s,DateTimeImmutable$n):void{$this->writes++;}public function saveDownload(int$a,?string$i,array$d,DateTimeImmutable$n):string{$this->writes++;return'10000000-0000-4000-8000-000000000001';}};$denied=new class implements PermissionCheckerInterface{public function allows(int$u,string$p):bool{return false;}};$storage=new CmsMediaStorage(sys_get_temp_dir().'/aims-cms-test',1024);$html=new SafeHtml();$safe=$html->sanitize('<div><script>alert(1)</script><p onclick="bad()">Hello <strong>world</strong> <a href="javascript:alert(1)" style="x">link</a></p></div>');$assert(!str_contains(strtolower($safe),'script')&&!str_contains(strtolower($safe),'onclick')&&!str_contains(strtolower($safe),'javascript:')&&str_contains($safe,'<strong>world</strong>'),'Unsafe CMS HTML survived sanitization.');$service=new CmsService($repo,$denied,$html,$storage);$assert($service->savePost(5,null,['post_type'=>'news','title'=>'Official Update','body_html'=>'<p>Body</p>','status'=>'draft'])->status===403&&$repo->writes===0,'Unauthorized CMS edit reached persistence.');
$editor=new class implements PermissionCheckerInterface{public function allows(int$u,string$p):bool{return$u===7&&$p==='cms.edit';}};$service=new CmsService($repo,$editor,$html,$storage);$draft=$service->savePost(7,null,['post_type'=>'news','title'=>'SEO Friendly Update','body_html'=>'<p onclick="x">Approved body</p>','status'=>'draft','meta_title'=>'Official meta title','meta_description'=>'Official description']);$assert($draft->successful&&$repo->last['slug']==='seo-friendly-update'&&!str_contains($repo->last['body_html'],'onclick'),'Draft editing or slug/sanitizer failed.');$assert(!$service->savePost(7,null,['post_type'=>'news','title'=>'Unauthorized Publish','body_html'=>'<p>Body</p>','status'=>'published'])->successful,'Editor without publish permission published content.');
$publisher=new class implements PermissionCheckerInterface{public function allows(int$u,string$p):bool{return$u===8&&in_array($p,['cms.edit','cms.publish'],true);}};$service=new CmsService($repo,$publisher,$html,$storage);$assert($service->saveFaq(8,null,['question'=>'How does membership work?','answer'=>'Use the verified application process.','display_order'=>'1','status'=>'published'])->successful,'Authorized FAQ publishing failed.');$upload=$service->upload(8,['error'=>UPLOAD_ERR_OK,'tmp_name'=>__FILE__,'name'=>'attack.php'],[]);$assert(!$upload->successful,'Non-HTTP or executable upload was accepted.');
$migration=(string)file_get_contents(dirname(__DIR__).'/database/migrations/20260905_260000_create_cms_tables.php');foreach(['CREATE TABLE pages','CREATE TABLE posts','CREATE TABLE media','CREATE TABLE faqs','CREATE TABLE downloads',"status IN ('draft','published','archived')",'uq_posts_type_slug','ENGINE=InnoDB']as$n)$assert(str_contains($migration,$n),"CMS migration missing {$n}.");$storageCode=(string)file_get_contents(dirname(__DIR__).'/app/Services/Cms/CmsMediaStorage.php');foreach(['is_uploaded_file','finfo','image/webp','application/pdf','random_bytes(24)']as$n)$assert(str_contains($storageCode,$n),"Upload protection missing {$n}.");$repoCode=(string)file_get_contents(dirname(__DIR__).'/app/Repositories/CmsRepository.php');foreach(["status='published'",'uniqueSlug','published_at<=CURRENT_TIMESTAMP','download_count=download_count+1']as$n)$assert(str_contains($repoCode,$n),"CMS publication control missing {$n}.");$seed=(string)file_get_contents(dirname(__DIR__).'/database/seeders/20260904_161000_rbac_seeder.php');$assert(str_contains($seed,"'content-manager' => ['cms.edit', 'cms.publish']"),'Content Manager permissions are not scoped separately.');echo"CMS checks passed: safe HTML, draft/publish authorization, SEO slugs/meta, protected media, publication filters, downloads, FAQs, and scoped Content Manager permissions.\n";}catch(Throwable$e){fwrite(STDERR,'CMS check failed: '.$e->getMessage().PHP_EOL);exit(1);}
