<?php declare(strict_types=1);
use App\Authorization\PermissionCheckerInterface;use App\Services\Events\EventRepositoryInterface;use App\Services\Events\EventService;
require dirname(__DIR__).'/vendor/autoload.php';
$assert=static function(bool$c,string$m):void{if(!$c)throw new RuntimeException($m);};
try{$eventId='10000000-0000-4000-8000-000000000001';$registrationId='20000000-0000-4000-8000-000000000001';
$repo=new class($eventId)implements EventRepositoryInterface{public int$writes=0;public string$last='';public string$lastEvent='';public string$lastRegistration='';public function __construct(private string$id){}public function publicEvents():array{return[['public_id'=>$this->id,'title'=>'Test <Event>','slug'=>'test-event']];}public function publicEvent(string$slug):?array{return$slug==='test-event'?['public_id'=>$this->id,'title'=>'Test Event']:null;}public function memberRegistrations(int$userId):array{return[];}public function register(?int$userId,string$eventPublicId,array$data,string$reference,DateTimeImmutable$now):array{$this->writes++;$this->last='register';return['registration_reference'=>$reference];}public function adminDashboard():array{return['types'=>[],'events'=>[]];}public function attendees(string$eventPublicId):?array{return['event'=>['public_id'=>$eventPublicId],'items'=>[]];}public function saveEvent(int$actor,?string$publicId,array$data):string{$this->writes++;$this->last='save';return$publicId??$this->id;}public function cancelEvent(int$actor,string$publicId,DateTimeImmutable$now):void{$this->writes++;$this->last='cancel_event';}public function cancelRegistration(int$actor,string$eventPublicId,string$registrationPublicId,DateTimeImmutable$now):void{$this->writes++;$this->last='cancel_registration';$this->lastEvent=$eventPublicId;$this->lastRegistration=$registrationPublicId;}public function recordAttendance(int$actor,string$eventPublicId,string$registrationPublicId,DateTimeImmutable$now):array{$this->writes++;$this->last='attend';$this->lastEvent=$eventPublicId;$this->lastRegistration=$registrationPublicId;return['idempotent'=>false];}};
$denied=new class implements PermissionCheckerInterface{public function allows(int$userId,string$permission):bool{return false;}};$service=new EventService($repo,$denied);$directory=$service->directory();$assert($directory['available']&&$directory['events'][0]['title']==='Test <Event>','Database-driven event directory failed.');$assert($service->event('invalid slug')===null,'Unsafe event slug was accepted.');$bad=$service->register(null,$eventId,['name'=>'A','email'=>'bad']);$assert($bad->status===422&&$repo->writes===0,'Invalid public registration reached persistence.');$valid=$service->register(null,$eventId,['name'=>'Public Guest','email'=>'guest@example.test']);$assert($valid->successful&&$repo->last==='register','Valid public registration failed.');$assert($service->adminDashboard(5)->status===403,'Event RBAC denial failed.');
$allowed=new class implements PermissionCheckerInterface{public function allows(int$userId,string$permission):bool{return$userId===9&&$permission==='event.manage';}};$service=new EventService($repo,$allowed);$invalidEvent=$service->saveEvent(9,null,['title'=>'Event','event_type'=>'bad','event_date'=>'bad','status'=>'published','registration_access'=>'public']);$assert($invalidEvent->status===422,'Invalid event data was accepted.');$validEvent=$service->saveEvent(9,null,['title'=>'Professional Workshop','event_type'=>'30000000-0000-4000-8000-000000000001','event_date'=>'2027-01-10','start_time'=>'10:00','end_time'=>'12:00','registration_deadline'=>'2027-01-09T17:00','status'=>'published','registration_access'=>'member','capacity'=>'100','fee'=>'0','fee_currency'=>'NGN','online_url'=>'https://example.test/event']);$assert($validEvent->successful&&$repo->last==='save','Valid event was not saved.');$assert($service->attend(9,$eventId,$registrationId)->successful&&$repo->last==='attend'&&$repo->lastEvent===$eventId&&$repo->lastRegistration===$registrationId,'Authorized attendance did not preserve the event-registration boundary.');
$throwing=new class implements EventRepositoryInterface{public function publicEvents():array{throw new RuntimeException('offline');}public function publicEvent(string$s):?array{throw new RuntimeException('offline');}public function memberRegistrations(int$i):array{return[];}public function register(?int$u,string$e,array$d,string$r,DateTimeImmutable$n):array{return[];}public function adminDashboard():array{return[];}public function attendees(string$e):?array{return null;}public function saveEvent(int$a,?string$i,array$d):string{return'';}public function cancelEvent(int$a,string$i,DateTimeImmutable$n):void{}public function cancelRegistration(int$a,string$e,string$i,DateTimeImmutable$n):void{}public function recordAttendance(int$a,string$e,string$i,DateTimeImmutable$n):array{return[];}};$assert(!(new EventService($throwing,$denied))->directory()['available'],'Unavailable event directory did not fail safely.');
$migration=(string)file_get_contents(dirname(__DIR__).'/database/migrations/20260905_220000_create_event_management_tables.php');foreach(['CREATE TABLE event_types','CREATE TABLE events','CREATE TABLE event_registrations','CREATE TABLE event_attendance','uq_event_registrations_event_email','uq_event_registrations_event_user','attendance_token_hash',"check_in_method IN ('manual','qr')",'ENGINE=InnoDB']as$n)$assert(str_contains($migration,$n),"Event migration missing {$n}.");$seed=(string)file_get_contents(dirname(__DIR__).'/database/seeders/20260905_220000_event_types_seeder.php');foreach(['Induction','Investiture','Conference','Workshop','Webinar','AGM','Training','Awards','Other']as$n)$assert(str_contains($seed,"'{$n}'"),"Event type missing {$n}.");$repoCode=(string)file_get_contents(dirname(__DIR__).'/app/Repositories/EventRepository.php');foreach(['FOR UPDATE','capacity','event.registration_created','event.attendance_recorded','idempotent','transaction(function','events.public_id=:event','event_public_id']as$n)$assert(str_contains($repoCode,$n),"Event safety control missing {$n}.");$routes=(string)file_get_contents(dirname(__DIR__).'/routes/web.php');foreach(['/events/{event}/register','/account/events/{event}/register','/admin/events','eventManage']as$n)$assert(str_contains($routes,$n),"Event route protection missing {$n}.");echo"Event management checks passed: database-driven types, public/member registration, duplicate/capacity controls, nested-ID binding, RBAC, attendance, QR readiness, and safe fallback.\n";
}catch(Throwable$e){fwrite(STDERR,'Event management check failed: '.$e->getMessage().PHP_EOL);exit(1);}
